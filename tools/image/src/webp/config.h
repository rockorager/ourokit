// Portable libwebp decoder configuration. SIMD is deliberately not enabled.
// HAVE_CONFIG_H prevents cpu.h from implicitly selecting unbuilt SIMD objects.
#ifndef OUROKIT_WEBP_CONFIG_H
#define OUROKIT_WEBP_CONFIG_H
#endif
